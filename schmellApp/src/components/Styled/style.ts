import {StyleSheet} from 'react-native';

const styledComponentStyles = StyleSheet.create({
  linearGradientBorder: {
    borderRadius: 100,
    height: 30,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});

export default styledComponentStyles;
